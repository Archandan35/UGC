> **Deploying?** Read [DEPLOY.md](./DEPLOY.md) for the complete Firebase + Railway walkthrough (Git push → Railway → Razorpay webhook → live).
>
> **New in this build:** single responsive exam page, per-question timing, persisted bookmarks (revision mode), PYQ mode, streaks & badges, and a real service layer (`services/gamificationService.js`, `bookmarksService.js`, `resultService.js`).

# Enterprise Mock Test Platform

Deploy-ready scalable starter with Firebase + Railway.
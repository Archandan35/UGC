/**
 * Smoke tests (placeholder) — not wired to a runner. The repo has no vitest
 * config yet; these files document the intended coverage so we can drop in
 * vitest later without restructuring.
 */
export const AUTH_TESTS_TODO = [
  "registerEmail creates a Firestore user doc",
  "loginEmail rejects bad credentials",
  "logout clears auth state",
];
